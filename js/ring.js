// class Ring {
//   constructor(container) {
//     this.container = container;
//     this.images = Array.prototype.slice.call(document.querySelectorAll("img")).map(function(element) {
//       return new Image(element, container);
//     });
//   }
// }
